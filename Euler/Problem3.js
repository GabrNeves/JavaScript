/*The prime factors of 13195 are 5, 7, 13 and 29.

What is the largest prime factor of the number 600851475143 ?*/

//code here

function simpleFunction(n) {
    n > 10 ? console.log(`${n} higher than 10`) : console.log(`${n} lower than 10`);
}

simpleFunction(1);